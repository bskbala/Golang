package main

import (
	"fmt"
	"reflect"
)

func main() {
	f := 5.1
	i := int(f) //Convert  Float to int

	fmt.Println(f, reflect.TypeOf(f))
	fmt.Println(i, reflect.TypeOf(i))

	//    Intergers=============
	var a unit16 = 0x10fe

	// Truncation

	b := uint16(b)
	fmt.Println(c)

}
